# 2026-08-12

## English Documentation Rewrite

- Rewrote `design/palette.md`, `design/office-map.md`, `design/screens.md`, and
  `guide/built-with-claude-code.md` fully in concise developer-documentation English.
- Preserved the original heading, table, code-fence, and ASCII-diagram structures, along with all palette
  tokens, hex values, workspace names and accents, dimensions, counts, seating and movement rules, and the
  five-line prompt pattern.
- Ran two independent adversarial reviews and corrected a seating-rule ambiguity, restored the work-log and
  single-line-warning qualifiers, and polished two guide phrases.
- Verified that the documents contain no Korean text, retain all exact inline literals and required facts, keep
  the ASCII diagram at 54 characters per line, and pass `git diff --check`.

## English Source Comment Translation

- Translated all 114 Korean comments in the eight requested TypeScript and TSX files into concise English;
  `src/main.tsx` contained no Korean comments and remained unchanged.
- Preserved all executable content, comment counts and delimiter kinds, indentation, line-ending patterns, and
  exact existing backtick references; unrelated working-tree edits were left untouched.
- Ran two adversarial reviews, corrected strict formatting and wording issues, and verified UTF-8 readability,
  zero Korean comment text, exact non-comment equivalence to `HEAD`, `npx tsc --noEmit`, and `git diff --check`.
